#include <ngx_core.h>
#include <netcdf.h>
#include "nc_util.h"
/*
nc_att_key_t *
nc_att_key_create(ngx_pool_t *pool) {
  nc_att_key_t *key = ngx_palloc(pool, sizeof(nc_att_key_t));
  key->name = ngx_palloc(pool, sizeof(char)*NC_MAX_NAME);
  return key;
}

nc_att_t *
nc_att_create(ngx_pool_t *pool) {
  nc_att_t *att = ngx_palloc(pool, sizeof(nc_att_t));
  att->key = nc_att_key_create(pool);
  return att;
}
*/
